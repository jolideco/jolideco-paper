name: jolideco-paper

channels:
    - defaults
    - conda-forge

variables:
    PYTHONNOUSERSITE: "1"

dependencies:
    - numpy
    - pip
    - python
    - astropy
    - scikit-image
    - scipy
    - gammapy
    -  pip:
        - showyourwork==0.4.2
        - pulp < 2.8
        - matplotlib
        - git+https://adonath:${JOLIDECO_ACCESS_TOKEN}@github.com/adonath/jolideco.git
